import java.util.Arrays;

public class test {
    public static void main (String[] args){
        
       int num = 2;

        String result = (num == 1) ? "A"
        : (num == 2) ? "B" 
        : (num == 3) ? "C"
        : "D";

        System.out.println(result);
       
    }
}
